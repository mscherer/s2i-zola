#!/bin/bash
set -e
echo "---> Installing application source ..."
cp -Rf /tmp/src/. .

echo "---> Build the website with zola"
OPTIONS=
[ ! -z WEBSITE_URL ] && OPTIONS="-u $WEBSITE_URL"
zola build $OPTIONS
